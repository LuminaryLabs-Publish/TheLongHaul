# Turn ledger: best-run persistence and projection

**Timestamp:** `2026-07-17T17-39-07-04-00`  
**Repository:** `LuminaryLabs-Publish/TheLongHaul`  
**Branch:** `main`

## Selection

- Enumerated 11 accessible `LuminaryLabs-Publish` repositories.
- Excluded `LuminaryLabs-Publish/TheCavalryOfRome`.
- Confirmed ten eligible central ledgers and root `.agent` states.
- Confirmed every eligible `main` head matched its documented repo-local head.
- Found no new, ledger-missing, root-agent-missing, undocumented or runtime-ahead repository.
- Selected only TheLongHaul using the oldest synchronized central timestamp: `2026-07-17T07-38-20-04-00`.

## Inspected

- `.agent` root routing, current audit, known gaps, next steps, validation and kit registry.
- `src/app/bootstrap.mjs`.
- Runtime chunks covering engine composition, result projection, scene flow, input, streaming, rendering and storage.
- `src/game/shared.mjs`, `src/game/cell-descriptor.mjs` and `src/game/score.mjs`.
- Current central ledger in `LuminaryLabs-Dev/LuminaryLabs`.

## Finding

The game creates a detailed `RunResult`, but `showResults()` stores one reduced global record under `the-long-haul-best-v2`, compares only `adjustedTime`, silently absorbs storage faults and never projects the restored record. The comparison scope, schema revision, durability result, migration and visible-frame convergence are not domain-owned.

## Added

- Timestamped project breakdown.
- Architecture, render, gameplay, interaction, best-run-system, deployment and central-sync audits.
- Refreshed required root `.agent` files and `kit-registry.json`.
- Central ledger reconciliation and internal change log.

## Not changed

Runtime, scoring, storage behavior, HTML, CSS, rendering, gameplay, tests, workflows and deployment.

## Branch policy

Direct `main` writes only. No branch and no pull request.