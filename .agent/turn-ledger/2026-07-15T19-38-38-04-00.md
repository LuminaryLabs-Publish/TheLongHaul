# Turn ledger: input action contract convergence

**Timestamp:** `2026-07-15T19-38-38-04-00`  
**Repository:** `LuminaryLabs-Publish/TheLongHaul`  
**Branch:** `main`

## Plan ledger

**Goal:** document and centrally reconcile the gap between declared Core Input actions and the executable browser keyboard path.

- [x] Compared all 11 accessible Publish repositories.
- [x] Excluded `TheCavalryOfRome`.
- [x] Confirmed ten eligible ledgers, root `.agent` states and synchronized heads.
- [x] Selected only TheLongHaul by the oldest synchronized timestamp.
- [x] Inspected the action manifest, bindings, contexts, browser listeners, held-key state and consumers.
- [x] Found direct host input bypass and the `KeyR` recovery/retry contradiction.
- [x] Preserved the complete kit and service census.
- [x] Added the timestamped tracker and focused audit family.
- [x] Refreshed required root `.agent` entry points and machine registry.
- [ ] Runtime implementation and executable browser fixtures remain future work.

## Finding

Core Input declares keyboard actions and contexts, but the host reads a separate mutable `keys` object and directly handles camera, map, pause and retry. The kit receives only a derived intent snapshot. `KeyR` is declared as recovery but executes retry; contextual `KeyE` performs recovery.

## Authority

`the-long-haul-input-action-contract-context-convergence-authority-domain`

## Change boundary

Documentation only. No runtime, input, gameplay, render, audio, storage, workflow or deployment behavior changed.