# Turn ledger: 2026-07-18T03-43-36-04-00

## Scope

Documentation-only breakdown of `LuminaryLabs-Publish/TheLongHaul` on `main`.

## Selection

- Compared all 11 repositories in `LuminaryLabs-Publish`.
- Excluded `LuminaryLabs-Publish/TheCavalryOfRome`.
- Confirmed all ten eligible repositories have central ledgers and root `.agent` state.
- Observed runtime-ahead heads in TheLongHaul, PrehistoricRush and HorrorCorridor.
- Selected only TheLongHaul because its runtime head was the most recently updated eligible undocumented delta.

## Runtime evidence reviewed

- Previous repo-local documentation head: `1ed59786aa8f8f26f643c9f1e8c4d0a4205181f6`.
- Reviewed runtime head: `753488e40e69fc13471df42959628ef3052e5992`.
- Delta: 14 commits.
- Primary files: `src/app/bootstrap.mjs`, `app-chunk-1.js`, `app-chunk-4.js`, `app-chunk-6.js`, `app-chunk-7.js`, `app-chunk-9.js`, `app-chunk-11.js`, `app-chunk-12.js`, `app-chunk-13.js`, `src/game/world-base.mjs`, `src/game/cell-descriptor.mjs`, and `tests/static-shell-smoke.mjs`.

## Finding

The new horizon provider writes updated patches into `horizonPatches`, while the Three.js reconciliation path realizes only cells without an existing host. An updated patch can therefore coexist with an older realized host. Because curved terrain heights depend on focus and policy, the runtime lacks explicit patch-to-host generation convergence.

The Horizon LOD DSK is also only partially projected: terrain resolution and `none` gates are consumed, but named road, settlement and forest presentation modes do not map to distinct render behavior.

## Documentation added

- timestamped project tracker;
- architecture DSK/domain map;
- render convergence audit;
- gameplay loop audit;
- interaction command/result audit;
- horizon-system contract audit;
- deployment fixture gate;
- central-sync reconciliation audit;
- this turn ledger.

## Documentation refreshed

- `.agent/START_HERE.md`
- `.agent/current-audit.md`
- `.agent/next-steps.md`
- `.agent/known-gaps.md`
- `.agent/validation.md`
- `.agent/kit-registry.json`

## Change boundary

No runtime, gameplay, rendering, test, workflow or deployment code was changed. No branch or pull request was created.
