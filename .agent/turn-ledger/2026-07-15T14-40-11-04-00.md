# Turn ledger: host clock fixed-step audit

**Timestamp:** `2026-07-15T14-40-11-04-00`  
**Repository:** `LuminaryLabs-Publish/TheLongHaul`  
**Branch:** `main`

## Plan ledger

**Goal:** document the current browser-clock path and define the smallest authority needed to separate callback cadence from gameplay time.

- [x] Compared the full 11-repository Publish inventory with the central ledger.
- [x] Excluded `TheCavalryOfRome`.
- [x] Confirmed ten eligible root `.agent` states and synchronized heads.
- [x] Selected only TheLongHaul by the oldest documented timestamp.
- [x] Inspected the RAF callback, `dt` derivation, driving preparation, engine tick and render path.
- [x] Preserved the complete 19-surface implementation inventory.
- [x] Added host-clock architecture, render, gameplay, interaction, clock, deployment and central-sync audits.
- [x] Refreshed the root `.agent` entry documents and machine registry.
- [ ] Runtime clock implementation and executable browser proof remain future work.

## Finding

`index.html` admits one variable delta per RAF callback:

```txt
rawDelta = max(0.001, callback delta)
dt = min(rawDelta, 1 / 15)
process driving once
engine.tick(dt) once
render once
```

No accumulator, fixed quantum, residual time, maximum-substep result, explicit overload receipt, visibility baseline, interpolation revision or first clock-bound frame acknowledgement exists.

## Result

Defined:

```txt
the-long-haul-host-clock-fixed-step-simulation-frame-authority-domain
```

No runtime, workflow or deployment behavior changed.
